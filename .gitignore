# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/

# Nix
result
result-*

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# API samples (contain real network data — keep local only)
api-samples/

# Secrets (never commit credentials)
*.env
!*.env.example

# VM test artifacts (large binary files — includes pfsense-pre-upgrade.qcow2 backup)
vm/*.iso
vm/*.iso.gz
vm/*.qcow2
vm/*.img
vm/*.img.gz

# VM debug logs (regenerated by expect scripts)
vm/*.log

# VM API key (contains test credentials)
vm/api-key.json
vm/api-key.txt

# VM upgrade sentinel
vm/upgrade-done

# Bank tester results (large, regenerated per run)
bank-tester/results/
